#ifndef functions_h
#define functions_h

#include <stdio.h>

float menu(int x, int choice);
void check(int *a);
void choiceCheck(int *a);

float rad(float x);
float sum(float x);
float area(float x);
float height(float x);
float value(float x);

#endif
